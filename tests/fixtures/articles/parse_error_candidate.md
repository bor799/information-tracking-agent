# Interesting But Parser-Breaking Candidate

This fixture has enough article text to fetch successfully. The LLM stub will return
malformed scoring JSON for this scenario so the pipeline can prove that parser errors
stop before Obsidian or Telegram output.
